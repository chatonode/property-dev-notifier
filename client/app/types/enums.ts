export const enum Route {
  Home = '/',
  Welcome = '/welcome',
  Signup = '/signup',
  Login = '/login',
  Logout = '/logout',
  // Dashboard = '/dashboard',
//   MyProfile = '/my-profile',
  Notify = '/notify',
}

export const enum EFormType {
  SIGNUP = 'signup',
  LOGIN = 'login',
}
